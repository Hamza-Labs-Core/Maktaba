package com.hamzalabs.maktaba.tv.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette. Deep near-black surfaces with a warm accent — readable
// across the room without glare.
val Brand = Color(0xFFE8B04B)      // warm gold accent
val Ink = Color(0xFF1A1206)        // text on the accent
val Background = Color(0xFF0B0B0F) // app background
val Surface = Color(0xFF17171D)    // cards / bars
val OnSurface = Color(0xFFECECF1)  // primary foreground text
