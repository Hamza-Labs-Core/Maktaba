# Paths here are anchored to this directory (apps/tv/).

# tvOS / SwiftPM build artifacts
tvos/.build/
tvos/.swiftpm/
tvos/Package.resolved

# Android / Gradle build artifacts (see also android/.gitignore)
android/.gradle/
android/build/
android/app/build/
android/local.properties
